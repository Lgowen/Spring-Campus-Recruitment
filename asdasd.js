// // const arr = []
// function poll(arr, num) {
    
// }

// // function offer(arr, num) {
    
// // }

// // let arr = ['1234','535','656565']
// // console.log(arr.sort((a,b) => a - b));


// // const arr = []
// // while((line = read_line()) != '') {
// //     const arr = line.split(' ')
// //     const order = arr[0]
// //     const num = arr[1]
// //     if(ru === 'offer'){
// //         arr.push(num)
// //         arr.sort((a,b) => a - b))
// //     }else{
// //         for(let i = 0; i < num; i++) {
// //             print(111)
// //         }
// //     }
// // }

// // const arr = line.split(' ')
// //     const order = arr[0]
// //     const num = arr[1]
// //     if(ru === 'offer'){
// //         arr.push(num)
// //         arr.sort((a,b) => a - b))
// //     }else {
// //         for(let i = 0; i < num; i++) {
// //             if(arr[i]){
// //                 print(arr[i])
// //             }else{
// //                 print(-1)
// //             }
// //         }
// //     }

    const data = [
        [21,5],
        [4,7],
        [5,9],
        [5,10]
    ]
    function getMax (arr) {
        const map = {}
        for(let i = 0; i < arr.leng)
        console.log(max, maxKey);
    }
    // console.log(getMax(data));
    getMax(data)

    
const arr = []
while((line = read_line()) != '') {
    const app = line.split(' ')
    if(app[0] === 'offer'){
        arr.push(app[1])
        arr.sort((a,b) => a - b)
    }else {
        for(let i = 0; i < app[1]; i++) {
            if(arr[0]){
                print(arr[0])
                arr.shift()
            }else{
                print(-1)
            }
        }
    }
}